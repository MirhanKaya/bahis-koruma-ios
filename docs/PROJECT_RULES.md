# Project Rules

- This repo is the single source of truth
- Do not rewrite the whole project
- Do not rename main folders
- Only work inside the assigned folder

Folders:
- mobile-ios = iOS app only
- admin-panel = admin panel only
- backend-api = backend only
- docs = documentation only

- Always list changed files
- Do not break existing structure
